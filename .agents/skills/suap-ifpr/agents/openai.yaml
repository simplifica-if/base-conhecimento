interface:
  display_name: "SUAP IFPR"
  short_description: "Consulta e navega no SUAP do IFPR"
  default_prompt: "Use $suap-ifpr para localizar o procedimento oficial ou consultar cargo, cursos e disciplinas de um docente no SUAP do IFPR."
policy:
  allow_implicit_invocation: true
